// ===== MOBILE-TOUCH-PRO.JS =====
// Interacciones táctiles profesionales para móvil - GameControl
// Autor: AI Assistant
// Versión: 1.0
// ================================================

class MobileTouchPro {
    constructor() {
        this.swipeThreshold = 50;
        this.tapThreshold = 300;
        this.doubleTapDelay = 300;
        this.longPressDelay = 500;
        this.pinchThreshold = 10;
        
        this.touches = {};
        this.lastTap = 0;
        this.gestureStartTime = 0;
        this.gestureData = {};
        
        this.isEnabled = this.isMobile();
        
        if (this.isEnabled) {
            this.init();
        }
    }
    
    init() {
        this.bindTouchEvents();
        this.setupGestureElements();
        this.addTouchStyles();
        this.initializeHaptics();
        this.setupAccessibility();
        
        console.log('👆 Mobile Touch Pro initialized');
    }
    
    bindTouchEvents() {
        // Eventos táctiles globales
        document.addEventListener('touchstart', (e) => {
            this.handleTouchStart(e);
        }, { passive: false });
        
        document.addEventListener('touchmove', (e) => {
            this.handleTouchMove(e);
        }, { passive: false });
        
        document.addEventListener('touchend', (e) => {
            this.handleTouchEnd(e);
        }, { passive: false });
        
        document.addEventListener('touchcancel', (e) => {
            this.handleTouchCancel(e);
        }, { passive: false });
        
        // Eventos específicos para elementos interactivos
        this.setupInteractiveElements();
    }
    
    handleTouchStart(e) {
        const touch = e.touches[0];
        const touchId = touch.identifier;
        const target = e.target;
        
        this.gestureStartTime = Date.now();
        
        // Guardar información del toque
        this.touches[touchId] = {
            startX: touch.clientX,
            startY: touch.clientY,
            currentX: touch.clientX,
            currentY: touch.clientY,
            startTime: this.gestureStartTime,
            target: target,
            moved: false,
            longPressTriggered: false
        };
        
        // Detectar elemento interactivo
        const interactiveElement = this.findInteractiveElement(target);
        if (interactiveElement) {
            this.handleInteractiveStart(interactiveElement, touch);
        }
        
        // Configurar long press timer
        this.setupLongPress(touchId, target);
        
        // Multi-touch gestures
        if (e.touches.length > 1) {
            this.handleMultiTouchStart(e);
        }
        
        // Prevenir comportamientos no deseados
        this.preventUnwantedBehavior(e, target);
    }
    
    handleTouchMove(e) {
        const touch = e.touches[0];
        const touchId = touch.identifier;
        const touchData = this.touches[touchId];
        
        if (!touchData) return;
        
        // Actualizar posición
        touchData.currentX = touch.clientX;
        touchData.currentY = touch.clientY;
        touchData.moved = true;
        
        // Calcular distancia
        const deltaX = touchData.currentX - touchData.startX;
        const deltaY = touchData.currentY - touchData.startY;
        const distance = Math.sqrt(deltaX * deltaX + deltaY * deltaY);
        
        // Cancelar long press si se movió mucho
        if (distance > 10) {
            this.cancelLongPress(touchId);
        }
        
        // Detectar swipe en progreso
        this.handleSwipeProgress(touchData, deltaX, deltaY);
        
        // Multi-touch gestures
        if (e.touches.length > 1) {
            this.handleMultiTouchMove(e);
        }
        
        // Scroll personalizado para elementos específicos
        this.handleCustomScroll(e, touchData);
    }
    
    handleTouchEnd(e) {
        const touch = e.changedTouches[0];
        const touchId = touch.identifier;
        const touchData = this.touches[touchId];
        
        if (!touchData) return;
        
        const duration = Date.now() - touchData.startTime;
        const deltaX = touchData.currentX - touchData.startX;
        const deltaY = touchData.currentY - touchData.startY;
        const distance = Math.sqrt(deltaX * deltaX + deltaY * deltaY);
        
        // Cancelar long press
        this.cancelLongPress(touchId);
        
        // Determinar tipo de gesto
        if (!touchData.moved && distance < 10) {
            // Tap o double tap\n            this.handleTap(touchData, duration);\n        } else if (distance > this.swipeThreshold) {\n            // Swipe\n            this.handleSwipe(touchData, deltaX, deltaY, duration);\n        }\n        \n        // Limpiar datos del toque\n        delete this.touches[touchId];\n        \n        // Multi-touch end\n        if (e.touches.length === 0) {\n            this.handleMultiTouchEnd(e);\n        }\n        \n        // Manejar elemento interactivo\n        const interactiveElement = this.findInteractiveElement(touchData.target);\n        if (interactiveElement) {\n            this.handleInteractiveEnd(interactiveElement, touchData);\n        }\n    }\n    \n    handleTouchCancel(e) {\n        // Limpiar todos los toques\n        Object.keys(this.touches).forEach(touchId => {\n            this.cancelLongPress(touchId);\n            delete this.touches[touchId];\n        });\n        \n        this.gestureData = {};\n    }\n    \n    handleTap(touchData, duration) {\n        const target = touchData.target;\n        const now = Date.now();\n        \n        // Detectar double tap\n        if (now - this.lastTap < this.doubleTapDelay) {\n            this.handleDoubleTap(target, touchData);\n            this.lastTap = 0; // Reset para evitar triple tap\n        } else {\n            // Single tap (con delay para verificar double tap)\n            setTimeout(() => {\n                if (now === this.lastTap) {\n                    this.handleSingleTap(target, touchData);\n                }\n            }, this.doubleTapDelay);\n            this.lastTap = now;\n        }\n    }\n    \n    handleSingleTap(target, touchData) {\n        // Agregar efecto visual\n        this.addTapEffect(target, touchData.startX, touchData.startY);\n        \n        // Haptic feedback\n        this.vibrate(30);\n        \n        // Dispatch evento personalizado\n        this.dispatchTouchEvent('mobileTap', target, {\n            x: touchData.startX,\n            y: touchData.startY,\n            target: target\n        });\n        \n        // Manejar elementos específicos\n        this.handleSpecificTap(target);\n    }\n    \n    handleDoubleTap(target, touchData) {\n        // Agregar efecto visual más prominente\n        this.addDoubleTapEffect(target, touchData.startX, touchData.startY);\n        \n        // Haptic feedback más fuerte\n        this.vibrate([50, 50, 50]);\n        \n        // Dispatch evento\n        this.dispatchTouchEvent('mobileDoubleTap', target, {\n            x: touchData.startX,\n            y: touchData.startY,\n            target: target\n        });\n        \n        // Funcionalidad específica para double tap\n        this.handleSpecificDoubleTap(target);\n    }\n    \n    handleLongPress(touchId, target) {\n        const touchData = this.touches[touchId];\n        if (!touchData || touchData.longPressTriggered) return;\n        \n        touchData.longPressTriggered = true;\n        \n        // Efectos visuales\n        this.addLongPressEffect(target);\n        \n        // Haptic feedback\n        this.vibrate(100);\n        \n        // Dispatch evento\n        this.dispatchTouchEvent('mobileLongPress', target, {\n            x: touchData.startX,\n            y: touchData.startY,\n            target: target,\n            duration: Date.now() - touchData.startTime\n        });\n        \n        // Funcionalidad específica\n        this.handleSpecificLongPress(target);\n    }\n    \n    handleSwipe(touchData, deltaX, deltaY, duration) {\n        const velocity = Math.sqrt(deltaX * deltaX + deltaY * deltaY) / duration;\n        const angle = Math.atan2(deltaY, deltaX) * 180 / Math.PI;\n        \n        // Determinar dirección\n        let direction;\n        if (Math.abs(deltaX) > Math.abs(deltaY)) {\n            direction = deltaX > 0 ? 'right' : 'left';\n        } else {\n            direction = deltaY > 0 ? 'down' : 'up';\n        }\n        \n        // Haptic feedback basado en velocidad\n        if (velocity > 0.5) {\n            this.vibrate(50);\n        }\n        \n        // Dispatch evento\n        this.dispatchTouchEvent('mobileSwipe', touchData.target, {\n            direction: direction,\n            deltaX: deltaX,\n            deltaY: deltaY,\n            velocity: velocity,\n            angle: angle,\n            duration: duration,\n            target: touchData.target\n        });\n        \n        // Manejar swipes específicos\n        this.handleSpecificSwipe(touchData.target, direction, velocity);\n    }\n    \n    setupLongPress(touchId, target) {\n        const timer = setTimeout(() => {\n            this.handleLongPress(touchId, target);\n        }, this.longPressDelay);\n        \n        if (this.touches[touchId]) {\n            this.touches[touchId].longPressTimer = timer;\n        }\n    }\n    \n    cancelLongPress(touchId) {\n        const touchData = this.touches[touchId];\n        if (touchData && touchData.longPressTimer) {\n            clearTimeout(touchData.longPressTimer);\n            delete touchData.longPressTimer;\n        }\n    }\n    \n    handleMultiTouchStart(e) {\n        if (e.touches.length === 2) {\n            const touch1 = e.touches[0];\n            const touch2 = e.touches[1];\n            \n            this.gestureData.pinch = {\n                initialDistance: this.getDistance(touch1, touch2),\n                initialScale: 1,\n                initialAngle: this.getAngle(touch1, touch2),\n                initialRotation: 0\n            };\n        }\n    }\n    \n    handleMultiTouchMove(e) {\n        if (e.touches.length === 2 && this.gestureData.pinch) {\n            e.preventDefault(); // Prevenir zoom del navegador\n            \n            const touch1 = e.touches[0];\n            const touch2 = e.touches[1];\n            \n            const currentDistance = this.getDistance(touch1, touch2);\n            const currentAngle = this.getAngle(touch1, touch2);\n            \n            const scale = currentDistance / this.gestureData.pinch.initialDistance;\n            const rotation = currentAngle - this.gestureData.pinch.initialAngle;\n            \n            // Dispatch eventos de pinch/zoom y rotación\n            if (Math.abs(scale - 1) > 0.1) {\n                this.dispatchTouchEvent('mobilePinch', e.target, {\n                    scale: scale,\n                    distance: currentDistance,\n                    initialDistance: this.gestureData.pinch.initialDistance\n                });\n            }\n            \n            if (Math.abs(rotation) > 10) {\n                this.dispatchTouchEvent('mobileRotate', e.target, {\n                    rotation: rotation,\n                    angle: currentAngle\n                });\n            }\n        }\n    }\n    \n    handleMultiTouchEnd(e) {\n        this.gestureData = {};\n    }\n    \n    setupInteractiveElements() {\n        // Elementos que necesitan interacciones especiales\n        const interactiveSelectors = [\n            'button',\n            '.btn',\n            'a',\n            '[role=\"button\"]',\n            '.clickable',\n            '.swipeable',\n            '.draggable',\n            '.card',\n            '.modal',\n            '.dropdown-toggle',\n            '.nav-link'\n        ];\n        \n        interactiveSelectors.forEach(selector => {\n            const elements = document.querySelectorAll(selector);\n            elements.forEach(element => {\n                this.setupInteractiveElement(element);\n            });\n        });\n        \n        // Observer para elementos dinámicos\n        this.setupInteractiveObserver();\n    }\n    \n    setupInteractiveElement(element) {\n        if (element.dataset.touchOptimized) return;\n        \n        element.dataset.touchOptimized = 'true';\n        \n        // Mejorar área de toque\n        const currentStyle = getComputedStyle(element);\n        const currentHeight = parseInt(currentStyle.height);\n        const currentPadding = parseInt(currentStyle.paddingTop) + parseInt(currentStyle.paddingBottom);\n        \n        if (currentHeight < 44 && currentPadding < 20) {\n            element.style.minHeight = '44px';\n            element.style.padding = '8px 12px';\n        }\n        \n        // Agregar clases para efectos\n        element.classList.add('touch-interactive');\n        \n        // Configurar gestos específicos\n        if (element.classList.contains('swipeable')) {\n            this.setupSwipeableElement(element);\n        }\n        \n        if (element.classList.contains('draggable')) {\n            this.setupDraggableElement(element);\n        }\n    }\n    \n    findInteractiveElement(target) {\n        // Buscar el elemento interactivo más cercano\n        let element = target;\n        while (element && element !== document.body) {\n            if (element.dataset.touchOptimized || \n                element.tagName === 'BUTTON' ||\n                element.classList.contains('btn') ||\n                element.tagName === 'A' ||\n                element.getAttribute('role') === 'button') {\n                return element;\n            }\n            element = element.parentElement;\n        }\n        return null;\n    }\n    \n    handleInteractiveStart(element, touch) {\n        // Agregar clase de estado activo\n        element.classList.add('touch-active');\n        \n        // Efectos visuales inmediatos\n        this.addPressEffect(element);\n    }\n    \n    handleInteractiveEnd(element, touchData) {\n        // Remover clase activa\n        element.classList.remove('touch-active');\n        \n        // Remover efectos visuales\n        setTimeout(() => {\n            this.removePressEffect(element);\n        }, 150);\n    }\n    \n    handleSpecificTap(target) {\n        // Funcionalidades específicas por tipo de elemento\n        if (target.classList.contains('sala-card')) {\n            this.handleSalaCardTap(target);\n        } else if (target.classList.contains('producto-item')) {\n            this.handleProductoTap(target);\n        } else if (target.classList.contains('sesion-item')) {\n            this.handleSesionTap(target);\n        }\n    }\n    \n    handleSpecificDoubleTap(target) {\n        // Double tap para acciones rápidas\n        if (target.classList.contains('sala-card')) {\n            this.handleSalaQuickAction(target);\n        } else if (target.classList.contains('timer-display')) {\n            this.handleTimerQuickEdit(target);\n        }\n    }\n    \n    handleSpecificLongPress(target) {\n        // Long press para menús contextuales\n        if (target.classList.contains('sala-card')) {\n            this.showSalaContextMenu(target);\n        } else if (target.classList.contains('sesion-item')) {\n            this.showSesionContextMenu(target);\n        } else if (target.classList.contains('producto-item')) {\n            this.showProductoContextMenu(target);\n        }\n    }\n    \n    handleSpecificSwipe(target, direction, velocity) {\n        // Swipes específicos\n        if (target.classList.contains('swipeable-card')) {\n            this.handleCardSwipe(target, direction);\n        } else if (target.classList.contains('modal')) {\n            this.handleModalSwipe(target, direction);\n        } else if (target.closest('.sidebar')) {\n            this.handleSidebarSwipe(direction, velocity);\n        }\n    }\n    \n    // Funciones específicas de la aplicación\n    handleSalaCardTap(target) {\n        const salaId = target.dataset.salaId;\n        if (salaId) {\n            this.dispatchTouchEvent('salaSelected', target, { salaId });\n        }\n    }\n    \n    handleSalaQuickAction(target) {\n        const salaId = target.dataset.salaId;\n        const estado = target.dataset.estado;\n        \n        if (estado === 'disponible') {\n            this.dispatchTouchEvent('salaQuickStart', target, { salaId });\n        } else if (estado === 'ocupada') {\n            this.dispatchTouchEvent('salaQuickEnd', target, { salaId });\n        }\n    }\n    \n    showSalaContextMenu(target) {\n        const salaId = target.dataset.salaId;\n        \n        // Crear menú contextual\n        const menu = this.createContextMenu([\n            { icon: 'play', text: 'Iniciar Sesión', action: 'start' },\n            { icon: 'pause', text: 'Pausar', action: 'pause' },\n            { icon: 'stop', text: 'Finalizar', action: 'end' },\n            { icon: 'plus', text: 'Agregar Tiempo', action: 'addTime' },\n            { icon: 'shopping-cart', text: 'Agregar Productos', action: 'addProducts' }\n        ]);\n        \n        this.showContextMenu(menu, target);\n    }\n    \n    handleCardSwipe(target, direction) {\n        if (direction === 'left') {\n            // Swipe left - acción de eliminación\n            this.addSwipeAction(target, 'delete', 'left');\n        } else if (direction === 'right') {\n            // Swipe right - acción de confirmación\n            this.addSwipeAction(target, 'confirm', 'right');\n        }\n    }\n    \n    handleModalSwipe(target, direction) {\n        if (direction === 'down') {\n            // Swipe down para cerrar modal\n            const modal = target.closest('.modal');\n            if (modal) {\n                const modalInstance = bootstrap.Modal.getInstance(modal);\n                if (modalInstance) {\n                    modalInstance.hide();\n                }\n            }\n        }\n    }\n    \n    handleSidebarSwipe(direction, velocity) {\n        if (window.mobileNavPro) {\n            if (direction === 'right' && velocity > 0.3) {\n                window.mobileNavPro.open();\n            } else if (direction === 'left' && velocity > 0.3) {\n                window.mobileNavPro.close();\n            }\n        }\n    }\n    \n    // Efectos visuales\n    addTapEffect(target, x, y) {\n        const ripple = document.createElement('div');\n        ripple.className = 'touch-ripple';\n        \n        const rect = target.getBoundingClientRect();\n        const size = Math.max(rect.width, rect.height);\n        \n        ripple.style.width = ripple.style.height = size + 'px';\n        ripple.style.left = (x - rect.left - size/2) + 'px';\n        ripple.style.top = (y - rect.top - size/2) + 'px';\n        \n        target.style.position = 'relative';\n        target.appendChild(ripple);\n        \n        setTimeout(() => {\n            if (ripple.parentNode) {\n                ripple.parentNode.removeChild(ripple);\n            }\n        }, 600);\n    }\n    \n    addDoubleTapEffect(target, x, y) {\n        // Efecto más prominente para double tap\n        this.addTapEffect(target, x, y);\n        \n        const pulse = document.createElement('div');\n        pulse.className = 'touch-double-pulse';\n        target.appendChild(pulse);\n        \n        setTimeout(() => {\n            if (pulse.parentNode) {\n                pulse.parentNode.removeChild(pulse);\n            }\n        }, 800);\n    }\n    \n    addLongPressEffect(target) {\n        target.classList.add('touch-long-press');\n        \n        setTimeout(() => {\n            target.classList.remove('touch-long-press');\n        }, 1000);\n    }\n    \n    addPressEffect(target) {\n        target.style.transform = 'scale(0.98)';\n        target.style.transition = 'transform 0.1s ease-out';\n    }\n    \n    removePressEffect(target) {\n        target.style.transform = '';\n        target.style.transition = '';\n    }\n    \n    addSwipeAction(target, action, direction) {\n        const actionButton = document.createElement('div');\n        actionButton.className = `swipe-action swipe-action-${action}`;\n        \n        const icon = action === 'delete' ? 'trash' : 'check';\n        actionButton.innerHTML = `<i class=\"fas fa-${icon}\"></i>`;\n        \n        target.style.position = 'relative';\n        target.appendChild(actionButton);\n        \n        // Animar entrada\n        setTimeout(() => {\n            actionButton.classList.add('swipe-action-visible');\n        }, 10);\n        \n        // Auto-remove después de un tiempo\n        setTimeout(() => {\n            if (actionButton.parentNode) {\n                actionButton.classList.remove('swipe-action-visible');\n                setTimeout(() => {\n                    if (actionButton.parentNode) {\n                        actionButton.parentNode.removeChild(actionButton);\n                    }\n                }, 300);\n            }\n        }, 3000);\n        \n        // Click en acción\n        actionButton.addEventListener('click', () => {\n            this.dispatchTouchEvent('swipeAction', target, { action, direction });\n            actionButton.parentNode.removeChild(actionButton);\n        });\n    }\n    \n    createContextMenu(items) {\n        const menu = document.createElement('div');\n        menu.className = 'touch-context-menu';\n        \n        items.forEach(item => {\n            const menuItem = document.createElement('div');\n            menuItem.className = 'context-menu-item';\n            menuItem.innerHTML = `\n                <i class=\"fas fa-${item.icon}\"></i>\n                <span>${item.text}</span>\n            `;\n            \n            menuItem.addEventListener('click', () => {\n                this.dispatchTouchEvent('contextMenuAction', menu, { action: item.action });\n                this.hideContextMenu();\n            });\n            \n            menu.appendChild(menuItem);\n        });\n        \n        return menu;\n    }\n    \n    showContextMenu(menu, target) {\n        // Ocultar menú existente\n        this.hideContextMenu();\n        \n        // Posicionar menú\n        const rect = target.getBoundingClientRect();\n        menu.style.position = 'fixed';\n        menu.style.left = rect.left + 'px';\n        menu.style.top = (rect.bottom + 10) + 'px';\n        menu.style.zIndex = '9999';\n        \n        // Agregar a body\n        document.body.appendChild(menu);\n        \n        // Animar entrada\n        setTimeout(() => {\n            menu.classList.add('context-menu-visible');\n        }, 10);\n        \n        // Click fuera para cerrar\n        const closeHandler = (e) => {\n            if (!menu.contains(e.target)) {\n                this.hideContextMenu();\n                document.removeEventListener('click', closeHandler);\n            }\n        };\n        \n        setTimeout(() => {\n            document.addEventListener('click', closeHandler);\n        }, 100);\n        \n        // Auto-cerrar después de un tiempo\n        setTimeout(() => {\n            this.hideContextMenu();\n        }, 10000);\n    }\n    \n    hideContextMenu() {\n        const existingMenu = document.querySelector('.touch-context-menu');\n        if (existingMenu) {\n            existingMenu.classList.remove('context-menu-visible');\n            setTimeout(() => {\n                if (existingMenu.parentNode) {\n                    existingMenu.parentNode.removeChild(existingMenu);\n                }\n            }, 300);\n        }\n    }\n    \n    addTouchStyles() {\n        if (document.getElementById('mobile-touch-styles')) return;\n        \n        const style = document.createElement('style');\n        style.id = 'mobile-touch-styles';\n        style.textContent = `\n            .touch-interactive {\n                -webkit-tap-highlight-color: transparent;\n                user-select: none;\n                -webkit-user-select: none;\n                touch-action: manipulation;\n            }\n            \n            .touch-active {\n                background-color: rgba(var(--primary-rgb), 0.1) !important;\n            }\n            \n            .touch-ripple {\n                position: absolute;\n                border-radius: 50%;\n                background: rgba(var(--primary-rgb), 0.3);\n                transform: scale(0);\n                animation: ripple 0.6s linear;\n                pointer-events: none;\n                z-index: 1000;\n            }\n            \n            @keyframes ripple {\n                to {\n                    transform: scale(4);\n                    opacity: 0;\n                }\n            }\n            \n            .touch-double-pulse {\n                position: absolute;\n                top: 50%;\n                left: 50%;\n                width: 20px;\n                height: 20px;\n                border: 2px solid var(--primary-color);\n                border-radius: 50%;\n                transform: translate(-50%, -50%) scale(0);\n                animation: doublePulse 0.8s ease-out;\n                pointer-events: none;\n                z-index: 1001;\n            }\n            \n            @keyframes doublePulse {\n                0% {\n                    transform: translate(-50%, -50%) scale(0);\n                    opacity: 1;\n                }\n                50% {\n                    transform: translate(-50%, -50%) scale(1.5);\n                    opacity: 0.7;\n                }\n                100% {\n                    transform: translate(-50%, -50%) scale(3);\n                    opacity: 0;\n                }\n            }\n            \n            .touch-long-press {\n                animation: longPressGlow 1s ease-in-out;\n            }\n            \n            @keyframes longPressGlow {\n                0%, 100% {\n                    box-shadow: 0 0 0 0 rgba(var(--primary-rgb), 0.4);\n                }\n                50% {\n                    box-shadow: 0 0 0 10px rgba(var(--primary-rgb), 0.1);\n                }\n            }\n            \n            .swipe-action {\n                position: absolute;\n                top: 50%;\n                right: 10px;\n                width: 40px;\n                height: 40px;\n                border-radius: 50%;\n                display: flex;\n                align-items: center;\n                justify-content: center;\n                color: white;\n                font-size: 16px;\n                cursor: pointer;\n                transform: translateY(-50%) scale(0);\n                transition: all 0.3s ease;\n                z-index: 1002;\n            }\n            \n            .swipe-action-delete {\n                background: var(--danger-color);\n            }\n            \n            .swipe-action-confirm {\n                background: var(--success-color);\n            }\n            \n            .swipe-action-visible {\n                transform: translateY(-50%) scale(1);\n            }\n            \n            .touch-context-menu {\n                background: var(--card-bg);\n                border-radius: 12px;\n                box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);\n                padding: 8px 0;\n                min-width: 200px;\n                opacity: 0;\n                transform: translateY(-10px) scale(0.9);\n                transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);\n                backdrop-filter: blur(10px);\n                -webkit-backdrop-filter: blur(10px);\n                border: 1px solid rgba(255, 255, 255, 0.1);\n            }\n            \n            .context-menu-visible {\n                opacity: 1;\n                transform: translateY(0) scale(1);\n            }\n            \n            .context-menu-item {\n                padding: 12px 16px;\n                display: flex;\n                align-items: center;\n                cursor: pointer;\n                transition: background-color 0.2s ease;\n                color: var(--text-color);\n            }\n            \n            .context-menu-item:hover {\n                background: rgba(var(--primary-rgb), 0.1);\n            }\n            \n            .context-menu-item i {\n                margin-right: 12px;\n                width: 16px;\n                color: var(--primary-color);\n            }\n            \n            .context-menu-item span {\n                font-size: 14px;\n                font-weight: 500;\n            }\n            \n            /* Mejoras para elementos específicos */\n            .btn.touch-interactive {\n                transition: all 0.2s ease;\n            }\n            \n            .btn.touch-active {\n                transform: scale(0.98);\n            }\n            \n            .card.touch-interactive {\n                transition: all 0.3s ease;\n            }\n            \n            .card.touch-active {\n                transform: translateY(-2px);\n                box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);\n            }\n            \n            /* Responsive adjustments */\n            @media (max-width: 768px) {\n                .touch-context-menu {\n                    max-width: calc(100vw - 32px);\n                }\n                \n                .context-menu-item {\n                    padding: 16px;\n                    min-height: 48px;\n                }\n                \n                .swipe-action {\n                    width: 48px;\n                    height: 48px;\n                    font-size: 18px;\n                }\n            }\n        `;\n        \n        document.head.appendChild(style);\n    }\n    \n    initializeHaptics() {\n        // Verificar soporte para vibración\n        this.hasVibration = 'vibrate' in navigator;\n        \n        if (this.hasVibration) {\n            console.log('👆 Haptic feedback available');\n        }\n    }\n    \n    vibrate(pattern) {\n        if (this.hasVibration && navigator.vibrate) {\n            navigator.vibrate(pattern);\n        }\n    }\n    \n    setupAccessibility() {\n        // Mejorar accesibilidad para elementos táctiles\n        document.addEventListener('keydown', (e) => {\n            if (e.key === 'Enter' || e.key === ' ') {\n                const activeElement = document.activeElement;\n                if (activeElement && activeElement.classList.contains('touch-interactive')) {\n                    e.preventDefault();\n                    this.simulateTap(activeElement);\n                }\n            }\n        });\n    }\n    \n    simulateTap(element) {\n        // Simular tap para accesibilidad\n        const rect = element.getBoundingClientRect();\n        const x = rect.left + rect.width / 2;\n        const y = rect.top + rect.height / 2;\n        \n        this.handleSingleTap(element, { startX: x, startY: y });\n    }\n    \n    setupInteractiveObserver() {\n        // Observer para elementos agregados dinámicamente\n        const observer = new MutationObserver((mutations) => {\n            mutations.forEach(mutation => {\n                mutation.addedNodes.forEach(node => {\n                    if (node.nodeType === Node.ELEMENT_NODE) {\n                        // Optimizar elementos interactivos nuevos\n                        const interactiveElements = node.querySelectorAll ?\n                            node.querySelectorAll('button, .btn, a, [role=\"button\"], .clickable') : [];\n                        \n                        interactiveElements.forEach(element => {\n                            this.setupInteractiveElement(element);\n                        });\n                        \n                        // Si el nodo es interactivo\n                        if (this.isInteractiveNode(node)) {\n                            this.setupInteractiveElement(node);\n                        }\n                    }\n                });\n            });\n        });\n        \n        observer.observe(document.body, {\n            childList: true,\n            subtree: true\n        });\n    }\n    \n    isInteractiveNode(node) {\n        return node.tagName === 'BUTTON' ||\n               node.classList.contains('btn') ||\n               node.tagName === 'A' ||\n               node.getAttribute('role') === 'button' ||\n               node.classList.contains('clickable');\n    }\n    \n    preventUnwantedBehavior(e, target) {\n        // Prevenir comportamientos no deseados en móvil\n        \n        // Prevenir selección de texto en elementos interactivos\n        if (this.findInteractiveElement(target)) {\n            e.preventDefault();\n        }\n        \n        // Prevenir zoom en double tap en elementos específicos\n        if (target.closest('.no-zoom') || target.classList.contains('btn')) {\n            e.preventDefault();\n        }\n    }\n    \n    handleSwipeProgress(touchData, deltaX, deltaY) {\n        // Mostrar feedback visual durante swipe\n        const target = touchData.target;\n        const swipeDirection = Math.abs(deltaX) > Math.abs(deltaY) ? 'horizontal' : 'vertical';\n        \n        if (target.classList.contains('swipeable-card') && swipeDirection === 'horizontal') {\n            const progress = Math.min(Math.abs(deltaX) / 100, 1);\n            const direction = deltaX > 0 ? 'right' : 'left';\n            \n            // Aplicar transformación visual\n            target.style.transform = `translateX(${deltaX * 0.3}px)`;\n            target.style.opacity = 1 - (progress * 0.3);\n            \n            // Mostrar indicador de acción\n            this.showSwipeIndicator(target, direction, progress);\n        }\n    }\n    \n    showSwipeIndicator(target, direction, progress) {\n        let indicator = target.querySelector('.swipe-indicator');\n        if (!indicator) {\n            indicator = document.createElement('div');\n            indicator.className = 'swipe-indicator';\n            target.appendChild(indicator);\n        }\n        \n        const icon = direction === 'left' ? 'trash' : 'check';\n        const color = direction === 'left' ? 'var(--danger-color)' : 'var(--success-color)';\n        \n        indicator.innerHTML = `<i class=\"fas fa-${icon}\"></i>`;\n        indicator.style.opacity = progress;\n        indicator.style.color = color;\n        indicator.style.position = 'absolute';\n        indicator.style.top = '50%';\n        indicator.style.transform = 'translateY(-50%)';\n        indicator.style[direction] = '20px';\n    }\n    \n    handleCustomScroll(e, touchData) {\n        // Scroll personalizado para elementos específicos\n        const scrollableElement = touchData.target.closest('.custom-scroll');\n        if (scrollableElement) {\n            e.preventDefault();\n            \n            const deltaY = touchData.currentY - touchData.startY;\n            scrollableElement.scrollTop -= deltaY * 0.5;\n        }\n    }\n    \n    // Funciones utilitarias\n    getDistance(touch1, touch2) {\n        const dx = touch1.clientX - touch2.clientX;\n        const dy = touch1.clientY - touch2.clientY;\n        return Math.sqrt(dx * dx + dy * dy);\n    }\n    \n    getAngle(touch1, touch2) {\n        const dx = touch1.clientX - touch2.clientX;\n        const dy = touch1.clientY - touch2.clientY;\n        return Math.atan2(dy, dx) * 180 / Math.PI;\n    }\n    \n    dispatchTouchEvent(eventName, target, detail = {}) {\n        const event = new CustomEvent(eventName, {\n            detail: {\n                target: target,\n                timestamp: Date.now(),\n                ...detail\n            },\n            bubbles: true,\n            cancelable: true\n        });\n        \n        target.dispatchEvent(event);\n    }\n    \n    isMobile() {\n        return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) ||\n               ('ontouchstart' in window) || (navigator.maxTouchPoints > 0);\n    }\n    \n    // API pública\n    enable() {\n        this.isEnabled = true;\n        console.log('👆 Mobile Touch Pro enabled');\n    }\n    \n    disable() {\n        this.isEnabled = false;\n        console.log('👆 Mobile Touch Pro disabled');\n    }\n    \n    addGestureListener(element, gesture, callback) {\n        element.addEventListener(`mobile${gesture.charAt(0).toUpperCase() + gesture.slice(1)}`, callback);\n    }\n    \n    removeGestureListener(element, gesture, callback) {\n        element.removeEventListener(`mobile${gesture.charAt(0).toUpperCase() + gesture.slice(1)}`, callback);\n    }\n    \n    getGestureState() {\n        return {\n            isEnabled: this.isEnabled,\n            activeTouches: Object.keys(this.touches).length,\n            hasVibration: this.hasVibration,\n            gestureData: this.gestureData\n        };\n    }\n    \n    destroy() {\n        // Limpiar todos los event listeners y efectos\n        Object.keys(this.touches).forEach(touchId => {\n            this.cancelLongPress(touchId);\n        });\n        \n        this.touches = {};\n        this.gestureData = {};\n        \n        // Remover estilos\n        const styles = document.getElementById('mobile-touch-styles');\n        if (styles) {\n            styles.remove();\n        }\n        \n        // Limpiar elementos optimizados\n        document.querySelectorAll('[data-touch-optimized]').forEach(element => {\n            element.removeAttribute('data-touch-optimized');\n            element.classList.remove('touch-interactive', 'touch-active');\n        });\n        \n        console.log('🗑️ Mobile Touch Pro destroyed');\n    }\n}\n\n// ===== AUTO-INICIALIZACIÓN =====\n\nfunction inicializarMobileTouchPro() {\n    if (window.mobileTouchPro) {\n        window.mobileTouchPro.destroy();\n    }\n    \n    window.mobileTouchPro = new MobileTouchPro();\n    return window.mobileTouchPro;\n}\n\n// Inicializar cuando DOM esté listo\nif (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', inicializarMobileTouchPro);\n} else {\n    inicializarMobileTouchPro();\n}\n\n// ===== EXPORTACIONES GLOBALES =====\n\nwindow.MobileTouchPro = MobileTouchPro;\nwindow.inicializarMobileTouchPro = inicializarMobileTouchPro;\n\nconsole.log('👆 Mobile Touch Pro module loaded');\n\n// ===== END MOBILE-TOUCH-PRO.JS =====
